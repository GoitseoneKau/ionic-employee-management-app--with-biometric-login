import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_emp_management',
  webDir: 'www',
  server:{
    cleartext:true
  }
};

export default config;
